<?xml version="1.0" encoding="UTF-8"?>
<grid-library>
	<grid-library-name>${project.artifactId}-enabler</grid-library-name>
	<grid-library-version>${project.version}</grid-library-version>

	<!-- No JRE arguments -->

	<dependency>
		<grid-library-name>${distribution.name}</grid-library-name>
	</dependency>

	<jar-path>
		<pathelement>ds_jars</pathelement>
	</jar-path>
	

	<!-- No conflicts -->

	<!-- No library paths -->

	<!-- No assembly paths -->

	<!-- No command paths -->

	<!-- No hooks paths -->

	<!-- No environment variables -->

	<!-- No system properties -->

</grid-library>
